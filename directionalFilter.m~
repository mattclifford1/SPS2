function directionalFilter(q,center,theta1,theta2)

dim = size(q);
center = (dim/2) + center;

u=20;v=20; atan(v/u)
for i = 1:dim(1)
    for j = 1:dim(2)
        if 
            q(i,j) = q(i,j);
        else
            q(i,j) = 1;
        end
    end
end


